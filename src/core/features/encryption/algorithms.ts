import { joinAlgorithmList } from './utils';

export enum ENCRYPTION_ALGORITHM {
	AES = 'AES',
	TWOFISH = 'Twofish',
}

export const AVAILABLE_ENCRYPTION_ALGORITHMS = [
	ENCRYPTION_ALGORITHM.AES,
	ENCRYPTION_ALGORITHM.TWOFISH,
	joinAlgorithmList([ENCRYPTION_ALGORITHM.AES, ENCRYPTION_ALGORITHM.TWOFISH]),
	joinAlgorithmList([ENCRYPTION_ALGORITHM.TWOFISH, ENCRYPTION_ALGORITHM.AES]),
];
