export enum ENCRYPTION_ALGORITHM {
	AES = 'AES',
	TWOFISH = 'Twofish',
}

export const ENCRYPTION_ALGORITHM_LIST = [
	ENCRYPTION_ALGORITHM.AES,
	ENCRYPTION_ALGORITHM.TWOFISH,
	`${ENCRYPTION_ALGORITHM.AES}-${ENCRYPTION_ALGORITHM.TWOFISH}`,
	`${ENCRYPTION_ALGORITHM.TWOFISH}-${ENCRYPTION_ALGORITHM.AES}`,
];
